#include <stdio.h>

/**
 * main - entry point of the program
 * Return: 0 for completion
 */

int main()
{
	char *eddie = "Edward Anan";
	char *pete = "Peter Odo";

	printf("Project done by %s and %s\n", eddie, pete);
	return (0);
}
